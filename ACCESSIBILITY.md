# Accessibility

The dashboard uses semantic landmarks, labeled search controls, status announcements, keyboard-friendly buttons, visible focus states from native controls, and responsive layouts. Future chart enhancements should preserve equivalent text summaries for non-visual users.
