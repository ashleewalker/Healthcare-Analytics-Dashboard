# Contributing

1. Use Node.js 20.
2. Install with `npm install`.
3. Run `npm test` before committing.
4. Run `npm run build` to verify the production bundle.
5. Keep demo data synthetic and never commit secrets or protected health information.
6. Keep commits focused and descriptive.
