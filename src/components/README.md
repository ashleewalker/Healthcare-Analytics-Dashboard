# UI components

Dashboard UI is intentionally kept lightweight. The current screen composes the KPI, chart, department, and patient patterns directly in `main.jsx`; this folder is reserved for future extraction when those patterns become reusable across multiple routes.
